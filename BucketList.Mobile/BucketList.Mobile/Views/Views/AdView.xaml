<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ads="clr-namespace:BucketList.Mobile.Ads"
             xmlns:controls="clr-namespace:MeetupManager.Controls;assembly=MeetupManager"
             x:Class="BucketList.Mobile.Views.Views.AdView">
    <ContentView.Content>
        <StackLayout>
            <!--<Grid RowSpacing="0" BackgroundColor="Red">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ListView Grid.Row="0"/>
                <ads:AdControlView Grid.Row="1"
                                   BackgroundColor="Blue"
                                   HeightRequest="50"/>
            </Grid>-->

            <ads:AdControlView HeightRequest="75"
                               VerticalOptions="End"/>

        </StackLayout>
    </ContentView.Content>
</ContentView>